{
    // POSITIONING
    "layer": "top",
    "position": "top",
    "mod": "dock",
    "exclusive": true,
    "passthrough": false,

    // STYLE
    "height": 30,
    "spacing": 0,

    // MODULES
    "modules-left": [
        "hyprland/workspaces",
        "custom/spotify"
    ],
    "modules-center": [
        "clock",
        "temperature"
    ],
    "modules-right": [
        "tray",
        "pulseaudio",
        "battery"
    ],

    // MODULE SETTINGS

    // Workspaces (for Hyprland)
    "hyprland/workspaces": {
        "format": "{icon}",
        "format-icons": {
            "1": "",
            "2": "",
            "3": "",
            "4": "",
            "5": "",
            "default": ""
        },
        "on-click": "activate",
        "persistent-workspaces": {
             "*": 5
        }
    },

    // Clock - Date and Time (FIXED: Replaced ':' with '%' in format strings)
    "clock": {
        "timezone": "America/Denver", // CHANGE THIS TO YOUR TIMEZONE
        "tooltip-format": "<big>{:%Y %B}</big>\n<small>{calendar}</small>",
        "format": "{:%b %d | %H:%M}",
        "format-alt": "{:%b %d %Y}",
        "interval": 1
    },

    // Media/Spotify (FIXED: Added 2>/dev/null to silence "No players found" spam)
    "custom/spotify": {
        "format": " {}",
        "interval": 1,
        "exec": "playerctl metadata --format '{{ title }} - {{ artist }}' -f '{{ status }}' 2>/dev/null | grep Playing | sed 's/Playing//g' | head -c 50",
        "tooltip": true
    },

    // System Tray (for app icons)
    "tray": {
        "spacing": 8
    },

    // Volume/Pulseaudio
    "pulseaudio": {
        "format": "{icon} {volume}%",
        "tooltip": false,
        "format-icons": {
            "default": "",
            "muted": ""
        },
        "on-click": "pavucontrol"
    },

    // Battery
    "battery": {
        "format": "{icon} {capacity}%",
        "format-icons": {
            "charging": "",
            "full": "",
            "default": ""
        },
        "tooltip-format": "{time}"
    },

    // Temperature (If you want it)
    "temperature": {
        "critical-threshold": 80,
        "format": "{temperatureC}°C {icon}",
        "format-icons": ["", "", ""],
        "interval": 5
    }
}
